# PXD023719

Held in sandbox until `parse_sdrf validate-sdrf --use_ols_cache_only` exits 0.

**Blocker:** parse_sdrf default treats pooled QC biological replicate as a warning/error (non-integer / <1).
