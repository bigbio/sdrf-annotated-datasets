# PXD038697

Held in sandbox until `parse_sdrf validate-sdrf --use_ols_cache_only` exits 0.

**Blocker:** comment[instrument] NT=Orbitrap Elite;AC=MS:1001910 fails OLS label match (canonical label is LTQ Orbitrap Elite).
