# PXD051882

Held in sandbox until `parse_sdrf validate-sdrf --use_ols_cache_only` exits 0.

**Blocker:** parse_sdrf default treats TMT pooled-channel biological replicate as a warning/error (non-integer / <1).
